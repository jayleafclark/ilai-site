---
// Ilai three-leaf sprout mark (recreated from the supplied brand logo) + wordmark.
// Two broad forest-green side leaves + a narrow upright power-pink centre leaf.
export interface Props {
  variant?: "ink" | "light";
}
const { variant = "ink" } = Astro.props;
const wordColor = variant === "light" ? "#ffffff" : "var(--ink)";
const src = `${import.meta.env.BASE_URL}${variant === "light" ? "logo-mark-light.png" : "logo-mark.png"}`;
---

<a href="/" class="logo" aria-label="Ilai Collective home">
  <img class="logo__mark" src={src} width="38" height="29" alt="" aria-hidden="true" />
  <span class="logo__word" style={`color:${wordColor}`}>ilai</span>
</a>

<style>
  .logo {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--ink);
  }
  .logo__mark {
    height: 29px;
    width: auto;
    display: block;
    transition: transform var(--dur-base) var(--ease);
  }
  .logo:hover .logo__mark {
    transform: translateY(-2px) scale(1.05);
  }
  .logo__word {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.7rem;
    letter-spacing: -0.03em;
  }
</style>
